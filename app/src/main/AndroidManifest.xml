<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.univealth">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TimeTable.ViewActivity"></activity>
        <activity android:name=".TimeTable.UpdateActivity"></activity>
        <activity android:name=".TimeTable.InsertActivity"></activity>
        <activity android:name=".Search.SearchDestActivity"></activity>
        <activity android:name=".Search.SearchAroundActivity"></activity>
        <activity android:name=".Intake.AddIntakeActivity"></activity>
        <activity android:name=".RouteActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>